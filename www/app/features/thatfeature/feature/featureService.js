(function () {
    'use strict';

    window.agApp.service('featureService', Service);

    function Service() {
        var service = {
            GetWeapon: GetWeapon
        };

        return service;

        function GetWeapon(TrapId) {
        }
    }
})();
                